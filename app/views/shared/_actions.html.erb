 <%= #link_to(object, class: 'btn btn-small', title: 'Show') do
 %>
  <!--<i class='icon-th-list'></i>-->
<% #end
%>

<% if current_user.admin? %>
  <%= link_to(send("edit_#{object.class.to_s.underscore}_path", object), class: 'btn btn-small', title: 'Edit') do %>
    <i class='icon-pencil'></i>
  <% end %>
  <%= link_to(object, class: 'btn btn-small', title: 'Delete', method: :delete, data: { confirm: 'Are you sure?' }) do %>
    <i class='icon-trash'></i>
  <% end %>
<% end %>